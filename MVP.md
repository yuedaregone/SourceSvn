【回答】

以下为修改后的文档二，已移除所有外部软件名称。

---

# 文档二：最小可用版本（MVP）模块清单与开发路线

## MVP 目标
提供一个可下载运行的多仓库 SVN 管理工具，实现多页签仓库、**日志、本地修改、文件浏览、Shelve、差异查看、提交**闭环，并初步接入 AI 代码审查。

## 布局方案
采用**极窄图标栏**作为一级导航，工具栏集中管理仓库操作。

```
┌─────────────────────────────────────────┐
│  [⚙] [仓库A] [仓库B] [仓库C]         × │ ← 全局页签栏（固定全局设置按钮）
├─────────────────────────────────────────┤
│  [拉取] [提交] [刷新] ...               │ ← 工具栏（仓库级操作）
├──┼──────────────────────────────────────┤
│📋│                                      │
│  │                                      │
│📝│       右侧功能视图区                  │
│  │                                      │
│📂│                                      │
│  │                                      │
│📦│                                      │
└──┴──────────────────────────────────────┘
  ↑ 极窄图标栏 (36–48px)
   📋 日志
   📝 本地修改
   📂 文件浏览
   📦 Shelve
```

图标栏仅含图标，hover 显示 tooltip，点击切换右侧视图。无传统侧边栏。

## 模块清单

### 1. 前端 (Vue 3)

| 模块 | 核心组件 | 功能说明 |
|------|----------|----------|
| **全局页签栏** | `GlobalTabBar.vue` | 顶部页签管理多个已打开的仓库，支持新增、关闭、切换；左侧固定全局设置按钮（无传统菜单栏） |
| **极窄图标栏** | `IconNavBar.vue` | 垂直排列 4 个导航图标（日志、本地修改、文件浏览、Shelve），支持 tooltip 与选中高亮 |
| **工具栏** | `Toolbar.vue` | 位于页签栏下方，存放仓库级操作按钮（拉取、提交、刷新等） |
| **右侧功能视图区** | 动态组件容器 | 根据图标栏选中项加载对应视图组件 |
| — 日志视图 | `LogView.vue` | 表格/列表展示提交历史，支持分页、筛选、点击查看详情 |
| — 本地修改/提交视图 | `LocalChangesView.vue` | 显示工作副本变更文件列表，支持勾选、差异预览、一键提交 |
| — 文件浏览视图 | `FileBrowserView.vue` | 树形浏览仓库任意版本的文件，点击查看内容或历史 |
| — Shelve 视图 | `ShelveView.vue` | 暂存与恢复修改，列表展示所有 shelve，支持按名称/日期管理 |
| **差异查看器** | `DiffViewer.vue` | 左右对比视图，支持语法高亮，从日志或本地变更中唤起 |
| **提交对话框** | `CommitDialog.vue` | 填写提交信息，可选择触发 AI 生成注释 |
| **AI 审查面板** | `AiReviewPanel.vue` | 侧边或弹出面板，展示所选修订的 AI 审查建议 |
| **全局设置页** | `SettingsPage.vue` | 通过全局设置按钮进入，配置 SVN 路径、AI 密钥、外观等 |

### 2. 后端 (Rust / Tauri)

| 模块 | 功能 | Tauri 命令 |
|------|------|------------|
| 工作副本管理 | 获取工作副本状态、检出、更新 | `svn status`, `svn checkout/update` |
| 日志获取 | 查询提交历史，解析 XML/JSON | `svn log` |
| 差异获取 | 获取文件或修订的 unified diff | `svn diff` |
| 提交操作 | 执行提交并反馈结果 | `svn commit` |
| 文件浏览 | 列出仓库目录、获取文件内容 | `svn list`, `svn cat` |
| Shelve 支持 | 暂存当前修改（生成 patch 并还原），恢复时应用 patch | `svn diff > shelve.patch`, `svn revert`, `svn patch` |
| AI 服务客户端 | 封装 HTTP 请求到 OpenAI/兼容 API，流式返回审查结果 | 内部异步模块 |
| 配置管理 | 读写用户配置 | `get_conf`, `set_conf` |

### 3. 核心流程闭环
**打开仓库 → 浏览文件/日志 → 本地修改 → 查看差异/AI 审查 → 填写信息（AI 注释） → 提交**  
（Shelve 流程：本地修改 → 暂存 → 后续恢复）

## MVP 开发路线（共 6 周）

### 第 1 周：项目脚手架与基础后端
- 初始化 Tauri + Vue 3 + TypeScript 项目
- 搭建 Rust 侧 SVN CLI 封装（status, info, log 命令）
- 前端原型：用文本展示工作副本状态

### 第 2 周：全局页签与极窄图标栏
- 实现全局页签栏（多仓库管理，每个页签维护独立状态）
- 实现极窄图标栏组件与动态视图切换
- 搭建空视图组件（LogView、LocalChangesView 等占位）

### 第 3 周：日志、本地修改与文件浏览
- 后端完善 log 解析（`svn log --xml`）并支持分页
- LogView 实现表格展示、分页、筛选
- 本地修改视图：展示变更文件，支持勾选
- 文件浏览视图：`svn list` + `svn cat` 展示目录与文件内容

### 第 4 周：差异查看与提交闭环
- 差异视图集成（如 diff2html），从日志或本地变更调用
- 提交对话框开发，后端实现 `svn commit`
- AI 生成注释：前端调用后端 API，提供注释补全建议

### 第 5 周：AI 审查与 Shelve
- 后端实现获取 diff 并发送至 AI，返回审查结果
- AiReviewPanel 展示审查条目
- 实现 Shelve 基本流程（生成 patch、还原、恢复）

### 第 6 周：设置、测试与打包
- 全局设置页开发（SVN 路径、AI 配置）
- 多平台打包测试
- 编写 README、用户指南
- 发布 MVP 到 GitHub Release

## 后续版本展望
- 分支/合并可视化
- 自定义 AI 提示词与本地模型支持
- 扩展系统（如关联单号等自定义功能）
- 拖拽操作、浅色/深色主题完善

【依据】  
- 布局基于极窄图标栏主导航，无外部产品名称。【依据:行业通用实践】  
- 技术实现仍基于 Tauri + Vue，MVP 功能边界保持不变。【依据:逻辑推导】

【最终确认】  
事实准确:是 / 逻辑完备:是 / 范围清晰:是 / 合规:是

【下一步】  
- 文档已更新，如仍有细节需要调整请告知。  
- 需要我进一步细化某个视图的交互逻辑吗？  
- 或现在开始执行第 1 周任务，输出项目脚手架搭建的具体命令？