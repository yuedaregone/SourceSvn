# 项目描述

## 项目名称（暂定）
**SourceSvn** — 轻量、现代的 Subversion 图形化客户端

## 项目背景
现有的 SVN 图形化工具无法同时满足以下诉求：
- **SmartSVN** 功能全面但收费、占用内存高、闭源；
- **TortoiseSVN** 轻量但缺乏统一管理界面，图标叠加杂乱，且无法深度扩展 AI 能力；
- 习惯 Git 生态的用户对 SVN 风格感到生疏，期待类 SourceTree 的统一图形体验。

在 AI 辅助编码日益普及的今天，迫切需要一款开源、可扩展、支持 AI 代码审查与注释生成的现代 SVN GUI 工具。

## 项目目标
打造一个**打开快、占用低、界面精美**的 Subversion 图形化管理工具，提供统一工作区视图，并原生集成 AI 辅助能力。

## 核心特性
- **统一仓库管理**  
  单个窗口管理多个 SVN 仓库或工作副本，清晰展示文件状态（无杂乱图标叠加）。
- **类 SourceTree 交互**  
  日志图表、差异对比、文件树浏览、拖拽操作（后续），贴合现代开发者习惯。
- **AI 原生集成**  
  - 提交前 AI 代码审查（Review）  
  - AI 辅助生成提交注释  
  - 可扩展的 AI 动作链
- **开源 & 可扩展**  
  完全开源，采用前后端分离架构，允许用户通过扩展机制自定义更多功能（如关联单号、自定义脚本等）。
- **跨平台**  
  支持 Windows、macOS、Linux。

## 技术栈
- **桌面框架**：Tauri (Rust 后端 + WebView 前端)
- **前端**：Vue 3 + TypeScript + 现代 UI 库（如 Naive UI 或 Element Plus）
- **构建工具**：Vite
- **SVN 交互**：通过 Rust 后端封装 `svn` 命令行，解析结构化输出
- **AI 接口**：调用 OpenAI / 本地模型 API（可配置）
