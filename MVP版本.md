# 最小可用版本（MVP）模块清单与开发路线

## MVP 目标
提供一个可下载运行的多仓库 SVN 管理工具，实现多页签仓库、**日志、本地修改、文件浏览、Shelve、差异查看、提交**闭环，并初步接入 AI 代码审查。

## 布局方案
采用**极窄图标栏**作为一级导航，工具栏集中管理仓库操作。

```
┌─────────────────────────────────────────┐
│  [⚙] [仓库A] [仓库B] [仓库C]         × │ ← 全局页签栏（固定全局设置按钮）
├─────────────────────────────────────────┤
│  [拉取] [提交] [刷新] ...               │ ← 工具栏（仓库级操作）
├──┼──────────────────────────────────────┤
│📋│                                      │
│  │                                      │
│📝│       右侧功能视图区                  │
│  │                                      │
│📂│                                      │
│  │                                      │
│📦│                                      │
└──┴──────────────────────────────────────┘
  ↑ 极窄图标栏 (36–48px)
   📋 日志
   📝 本地修改
   📂 文件浏览
   📦 Shelve
```

图标栏仅含图标，hover 显示 tooltip，点击切换右侧视图。无传统侧边栏。

## 模块清单

### 1. 前端 (Vue 3)

| 模块 | 核心组件 | 功能说明 |
|------|----------|----------|
| **全局页签栏** | `GlobalTabBar.vue` | 顶部页签管理多个已打开的仓库，支持新增、关闭、切换；左侧固定全局设置按钮（无传统菜单栏） |
| **极窄图标栏** | `IconNavBar.vue` | 垂直排列 4 个导航图标（日志、本地修改、文件浏览、Shelve），支持 tooltip 与选中高亮 |
| **工具栏** | `Toolbar.vue` | 位于页签栏下方，存放仓库级操作按钮（拉取、提交、刷新等） |
| **右侧功能视图区** | 动态组件容器 | 根据图标栏选中项加载对应视图组件 |
| — 日志视图 | `LogView.vue` | 表格/列表展示提交历史，支持分页、筛选、点击查看详情 |
| — 本地修改/提交视图 | `LocalChangesView.vue` | 显示工作副本变更文件列表，支持勾选、差异预览、一键提交 |
| — 文件浏览视图 | `FileBrowserView.vue` | 树形浏览仓库任意版本的文件，点击查看内容或历史 |
| — Shelve 视图 | `ShelveView.vue` | 暂存与恢复修改，列表展示所有 shelve，支持按名称/日期管理 |
| **差异查看器** | `DiffViewer.vue` | 左右对比视图，支持语法高亮，从日志或本地变更中唤起 |
| **提交对话框** | `CommitDialog.vue` | 填写提交信息，可选择触发 AI 生成注释 |
| **AI 审查面板** | `AiReviewPanel.vue` | 侧边或弹出面板，展示所选修订的 AI 审查建议 |
| **全局设置页** | `SettingsPage.vue` | 通过全局设置按钮进入，配置 SVN 路径、AI 密钥、外观等 |

### 2. 后端 (Rust / Tauri)

| 模块 | 功能 | Tauri 命令 |
|------|------|------------|
| 工作副本管理 | 获取工作副本状态、检出、更新 | `svn status`, `svn checkout/update` |
| 日志获取 | 查询提交历史，解析 XML/JSON | `svn log` |
| 差异获取 | 获取文件或修订的 unified diff | `svn diff` |
| 提交操作 | 执行提交并反馈结果 | `svn commit` |
| 文件浏览 | 列出仓库目录、获取文件内容 | `svn list`, `svn cat` |
| Shelve 支持 | 暂存当前修改（生成 patch 并还原），恢复时应用 patch | `svn diff > shelve.patch`, `svn revert`, `svn patch` |
| AI 服务客户端 | 封装 HTTP 请求到 OpenAI/兼容 API，流式返回审查结果 | 内部异步模块 |
| 配置管理 | 读写用户配置 | `get_conf`, `set_conf` |

### 3. 核心流程闭环
**打开仓库 → 浏览文件/日志 → 本地修改 → 查看差异/AI 审查 → 填写信息（AI 注释） → 提交**  
（Shelve 流程：本地修改 → 暂存 → 后续恢复）

## MVP 开发路线（共 6 周）

# SourceSvn 详细设计任务（6周）

## 第 1 周：项目脚手架与基础后端

### 目标
- 完成项目环境搭建，前后端可通信
- 后端能调用 `svn` 命令并返回结果到前端

### 任务分解
1. **初始化 Tauri + Vue 3 + TypeScript 项目**
   - 使用 `create-tauri-app` 或手动搭建
   - 配置 Vite，确保 HMR 可用
   - 验证 Tauri 开发模式可启动窗口
2. **搭建 Rust 后端基础结构**
   - 创建 `svn` 模块，封装命令调用函数 `run_svn(args: &[&str]) -> Result<String>`
   - 实现 Tauri 命令 `svn_status(repo_path: &str)`，返回工作副本状态文本
   - 实现 `svn_info(repo_path: &str)`，返回仓库基本信息（URL、版本号等）
3. **前端基础原型**
   - 简单页面：输入仓库路径，调用后端命令，将返回结果显示在 `<pre>` 中
   - 验证前后端通信正常
4. **开发环境配置**
   - 配置 ESLint、Prettier
   - 添加 Rust 端测试框架（如 `#[cfg(test)]`）

### 验收标准
- 可从界面输入工作副本路径，点击按钮显示 `svn status` 和 `svn info` 结果
- 测试用例覆盖命令解析失败场景

## 第 2 周：全局页签与极窄图标栏

### 目标
- 实现多仓库页签管理，每个页签保持独立状态
- 实现极窄图标栏与动态视图切换

### 任务分解
1. **全局页签栏组件 (`GlobalTabBar.vue`)**
   - 数据结构：`tabs: Ref<Tab[]>`，其中 `Tab` 包含 `id, repoPath, title, icon?`
   - 支持新增页签（“打开仓库”对话框 -> 检出或选择已有路径）
   - 支持关闭页签，当前激活页签高亮
   - 左侧固定全局设置按钮（齿轮图标）
   - 关闭页签时清理关联状态
2. **极窄图标栏组件 (`IconNavBar.vue`)**
   - 垂直排列 4 个图标按钮，点击触发 `activeView` 切换
   - 图标使用 MIT 许可图标库（如 `@tabler/icons-vue` 或 `lucide-vue`）
   - 实现 tooltip （Tippy.js 或纯 CSS）
   - 当前视图高亮样式
3. **视图容器与路由**
   - 使用 `<component :is="currentViewComponent">` 动态加载视图
   - 每个视图为独立 Vue 组件，目前用占位符
4. **基础状态管理**
   - 使用 `pinia` 或 `provide/inject`
   - 每个页签持有自己的 `currentView` 状态

### 验收标准
- 可打开多个仓库页签并切换
- 图标栏切换视图时，右侧区域显示对应占位视图
- 关闭页签后，该仓库状态释放

## 第 3 周：日志、本地修改与文件浏览

### 目标
- 实现三个核心视图的全部主要功能，与后端联动

### 任务分解
1. **后端扩展**
   - `svn_log` 命令：解析 `--xml` 输出，返回结构化数据（版本号、作者、日期、消息）
   - `svn_status` 增强：解析 `--xml` 输出，区分修改类型（M/A/D/?等）
   - `svn_list` 命令：列出仓库目录，递归或非递归
   - `svn_cat` 命令：获取文件内容
   - 数据格式使用 JSON 传递到前端
2. **日志视图 (`LogView.vue`)**
   - 顶部筛选栏：作者、日期范围、关键字搜索（前端过滤或重新请求）
   - 表格/列表展示：列：版本号、作者、日期、提交信息摘要
   - 点击行展开详细信息（完整消息、变更文件列表）
   - 分页或无限滚动，加载更多
3. **本地修改视图 (`LocalChangesView.vue`)**
   - 获取工作副本状态，展示变更文件列表（图标+文件名+状态）
   - 支持勾选文件，用于后续提交
   - 点击文件可预览差异（调用差异查看器）
   - 显示统计：新增/修改/删除文件数
4. **文件浏览视图 (`FileBrowserView.vue`)**
   - 树形组件（`el-tree` 或自定义），根节点为仓库根
   - 点击目录展开，调用 `svn_list` 获取子项
   - 点击文件显示文件内容（代码块），支持语法高亮（`highlight.js` 或 `codemirror`）
   - 支持切换浏览版本（默认 HEAD）

### 验收标准
- 日志列表可正常展示指定仓库的提交历史
- 本地修改视图正确显示工作副本变更
- 文件浏览可展开目录，查看文件内容

## 第 4 周：差异查看与提交闭环

### 目标
- 完成差异对比视图
- 完成提交流程，集成 AI 注释生成

### 任务分解
1. **差异查看器 (`DiffViewer.vue`)**
   - 使用 `diff2html` 库，将 unified diff 渲染为美观的 side-by-side 或 unified 视图
   - 后端提供 `svn_diff` 命令：支持指定文件、指定版本范围
   - 从日志视图双击文件或本地修改视图点击文件时，弹出差异模态框或右侧面板
2. **提交对话框 (`CommitDialog.vue`)**
   - 从本地修改视图点击“提交”按钮打开
   - 展示已选文件列表，不可选（若有勾选则显示勾选的文件）
   - 输入框：提交信息（多行文本框）
   - 底部按钮：提交、取消、AI 生成注释
3. **AI 生成注释**
   - 后端命令 `generate_commit_message(diff_content: &str)`：调用 OpenAI API（可配置），使用 prompt 要求生成 concise commit message
   - 前端请求后，显示 loading，将结果填入文本框
   - 处理 API 错误与超时
4. **提交执行**
   - 后端 `svn_commit(path: &str, message: &str, files: Vec<&str>)`：执行 `svn commit -m "..." file1 file2 ...`
   - 前端显示执行结果（成功/失败，错误信息）
   - 提交成功后刷新本地修改视图

### 验收标准
- 差异视图可正确显示文件变更
- 可从本地修改视图发起提交，填写信息并成功提交
- AI 生成注释功能可用（需配置有效 API Key）

## 第 5 周：AI 代码审查与 Shelve

### 目标
- 完成 AI 代码审查面板
- 实现 Shelve 机制

### 任务分解
1. **AI 代码审查 (`AiReviewPanel.vue`)**
   - 触发方式：日志视图或本地修改视图选择修订/文件后，点击“AI 审查”按钮
   - 后端命令 `review_changes(diff_content: &str)`：发送 diff 到 AI，prompt 要求代码审查（潜在问题、建议）
   - 前端展示：审查结果可逐条显示（如列表或对话形式）
   - 支持流式返回（Tauri 异步事件），逐步显示内容
2. **Shelve 功能**
   - 后端 `shelve_save(repo_path: &str, name: &str)`：执行 `svn diff > .shelves/name.patch`，然后 `svn revert -R .`（需确认）
   - 后端 `shelve_list(repo_path: &str)`：扫描 `.shelves` 目录，返回列表（名称、日期）
   - 后端 `shelve_apply(repo_path: &str, name: &str)`：应用 patch `svn patch .shelves/name.patch`
   - 后端 `shelve_delete(repo_path: &str, name: &str)`：删除 patch 文件
   - 前端 `ShelveView.vue`：展示 shelve 列表，支持保存当前修改、应用、删除 shelve
   - 保存时需输入名称

### 验收标准
- 可选择某个提交或修改发送 AI 请求，审查结果可读
- Shelve 可保存当前工作副本修改，文件恢复干净；可恢复 shelve

## 第 6 周：设置、测试与打包

### 目标
- 完善全局设置页面
- 多平台打包并发行 MVP

### 任务分解
1. **全局设置页 (`SettingsPage.vue`)**
   - 从全局按钮进入，可返回
   - 配置项：
     - SVN 可执行文件路径（或自动查找）
     - AI 服务：API 端点、Key、模型名称
     - 外观：浅色/深色主题（可先实现简单切换）
     - 其他：语言（预留）
   - 使用 Tauri 的 `fs` 或 `confy` 存储配置
2. **多平台测试**
   - Windows：验证打包后的 MSI/EXE
   - macOS：验证 DMG
   - Linux：验证 AppImage 或 deb
   - 测试安装、启动、基本功能
3. **文档编写**
   - README.md：项目简介、功能、安装方式、构建步骤、截图
   - CONTRIBUTING.md（可选）
   - 用户指南：基本使用说明
4. **发布**
   - 推送到 GitHub Public Repository
   - 配置 GitHub Actions 自动构建发布
   - 创建 MVP Release，附带二进制文件

## 后续版本展望
- 分支/合并可视化
- 自定义 AI 提示词与本地模型支持
- 扩展系统（如关联单号等自定义功能）
- 拖拽操作、浅色/深色主题完善
